# Environment variables for MordomoZAP (example)

# Gemini API key for browser builds (injected via Vite define in vite.config.ts)
# Create a .env.local with the real value and rebuild the app.
GEMINI_API_KEY=

# OpenAI API key for browser builds (use only in local/dev; avoid exposing in production)
# Create a .env.local with the real value and rebuild the app.
OPENAI_API_KEY=

# Notes:
# - For production, prefer moving AI calls to backend and keep the key server-side.